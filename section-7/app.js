// Maximilliann Schwarzmüller did not even use this
// file in his demonstration, but rather only the
// JavaScript console window. I will demonstrate the
// extraction of a simple piece of information from
// the top level of the DOM--Window.

console.dir(window.navigator.platform);
console.dir(window.navigator.plugins);